var mList=[
    "Asia",
    "India",
    "Madhya Pradesh",
    "Dewas",
    "Khategaon",
    455336
];

mList.forEach(element => { //require callback
    console.log(element);
}); //forEach loops through each element of the array or list
//or
List.forEach((element) => console.log(element));
//or
mList.forEach((element) => (console.log(element)));